Install elm core packages
elm package install

Start reactor
elm reactor -a=localhost

Howto make a module
add the module directive on top of the module file
e.g. module MyModule where
remove the main function and data definition elements

Howto add a custom font
During development just add it to the operating system
In production add it to your main html page in the style tag



